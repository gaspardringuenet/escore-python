[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "escore-python"
version = "0.0.0"

[tool.setuptools]
packages = ["src"]

[tool.setuptools.package-dir]
"" = "."